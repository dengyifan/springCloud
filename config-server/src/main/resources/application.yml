spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/dengyifan/springCloud
          search-paths: config-repo
server:
  port: 7001